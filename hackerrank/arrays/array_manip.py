def arrayManipulation(n, queries):
    '''
    Question: Starting with a 1-indexed array of zeros and a list of operations, 
    for each operation add a value to each of the array element between two given indices, inclusive. 
    Once all operations have been performed, return the maximum value in the array.

    input
    int n - the number of elements in the array
    int queries[q][3] - 2-d array of queries where each queries[i] contains 3 ints a,b,k

    output
    int - the maximum value in the resultant array
    '''
    from collections import Counter
    c = Counter()
    
    print (arr, a, b, k)
'''
Incomplete!!!!! 

'''

n = 10
queries = [[1,5,3], [4,8,7], [6,9,1]]
arrayManipulation(n, queries)